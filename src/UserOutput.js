import React from "react";

const useroutput = props => {
  return (
    <div>
      <p>This is {props.username}</p>
      <p>This is {props.username} again!!</p>
    </div>
  );
};
export default useroutput;
